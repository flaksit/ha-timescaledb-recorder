---
phase: 03-hardening-and-observability
plan: 08
type: execute
wave: 6
depends_on: [03-01, 03-02, 03-03, 03-04, 03-05, 03-06, 03-07]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [SQLERR-03, SQLERR-04, WATCH-01, WATCH-03, OBS-01, OBS-02, OBS-03, OBS-04]
must_haves:
  truths:
    - "REQUIREMENTS.md marks SQLERR-03 and SQLERR-04 with DROPPED status and a pointer to 03-CONTEXT.md#D-01"
    - "ROADMAP.md Phase 3 success criterion #3 (per-row fallback) is rewritten to reflect the unified-retry + stall-notification story"
    - "ROADMAP.md Phase 3 success criterion #4 (code-bug repair issue) is rewritten to reference stall notification after N=5 retries + worker_stalled repair issue that auto-clears"
    - "ROADMAP.md Phase 3 plans list populated with the eight plans shipped"
    - "REQUIREMENTS.md traceability table updated: WATCH-01, WATCH-03, OBS-01, OBS-02, OBS-03, OBS-04 marked as shipped; SQLERR-03, SQLERR-04 marked DROPPED"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "updated DROPPED markers for SQLERR-03/04 + traceability status"
      contains: "DROPPED"
    - path: ".planning/ROADMAP.md"
      provides: "rewritten Phase 3 goal + success criteria reflecting shipped architecture"
      contains: "worker_stalled"
  key_links:
    - from: "REQUIREMENTS.md SQLERR-03 / SQLERR-04 entries"
      to: ".planning/phases/03-hardening-and-observability/03-CONTEXT.md D-01"
      via: "inline reference"
      pattern: "03-CONTEXT\\.md#D-01"
---

<objective>
Record the Phase 3 scope decisions in the project's source-of-truth documents so future work sees the DROPPED status of SQLERR-03/04 and the rewritten success criteria without having to re-read the Phase 3 CONTEXT.md.

Purpose: Without this doc update, REQUIREMENTS.md would still show SQLERR-03/04 as "Pending" forever, and ROADMAP.md would still describe success criteria (per-row fallback, code-bug halt) that Phase 3 intentionally did not build. Anyone coming back to this code would need to know to read 03-CONTEXT.md to understand why those behaviours are absent.

Output: Updated REQUIREMENTS.md and ROADMAP.md reflecting what v1.1 Phase 3 actually shipped. Purely documentation work — no code, no tests.

Implements: D-01-b (REQUIREMENTS.md SQLERR-03/04 marked DROPPED with pointer to D-01), D-01-c (ROADMAP.md Phase 3 success criteria rewritten), D-12 (requirement remapping).

Rationale for including SQLERR-03 and SQLERR-04 in this plan's `requirements` field: per the planning-context instruction, every requirement ID for the phase MUST appear in at least one plan. SQLERR-03 and SQLERR-04 are DROPPED, not implemented — Plan 08 is where that drop is recorded in the source-of-truth documents. Listing them here is how Phase 3 "addresses" them (per the planning note: "they must still appear in at least one plan's requirements field with a note that they are addressed via the unified retry + stall notification approach").
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-hardening-and-observability/03-CONTEXT.md
@.planning/phases/03-hardening-and-observability/03-01-SUMMARY.md
@.planning/phases/03-hardening-and-observability/03-02-SUMMARY.md
@.planning/phases/03-hardening-and-observability/03-03-SUMMARY.md
@.planning/phases/03-hardening-and-observability/03-04-SUMMARY.md
@.planning/phases/03-hardening-and-observability/03-05-SUMMARY.md
@.planning/phases/03-hardening-and-observability/03-06-SUMMARY.md
@.planning/phases/03-hardening-and-observability/03-07-SUMMARY.md

<interfaces>
<!-- Existing REQUIREMENTS.md entries to update (lines 54-55) -->

Current:
```markdown
- [ ] **SQLERR-03**: DataError / IntegrityError → isolate offending row via single-row fallback loop; log + skip bad row; continue batch
- [ ] **SQLERR-04**: ProgrammingError / SyntaxError → critical log + repair issue; do not retry (code bug or schema drift)
```

Target (DROPPED marker + rationale pointer per D-01-b):

```markdown
- ~~**SQLERR-03**~~: ~~DataError / IntegrityError → isolate offending row via single-row fallback loop; log + skip bad row; continue batch~~ **DROPPED** — see [Phase 3 CONTEXT D-01](phases/03-hardening-and-observability/03-CONTEXT.md). Persistent bad-row failures stall the worker and surface via the `worker_stalled` repair issue + Phase 2 stall notification.
- ~~**SQLERR-04**~~: ~~ProgrammingError / SyntaxError → critical log + repair issue; do not retry (code bug or schema drift)~~ **DROPPED** — see [Phase 3 CONTEXT D-01](phases/03-hardening-and-observability/03-CONTEXT.md). Persistent code-bug / schema-drift failures follow the same unified path as other errors: retry → stall notification after N=5 → `worker_stalled` repair issue.
```

<!-- Existing ROADMAP.md Phase 3 success criteria (lines 73-79) -->

Current:
```markdown
**Success Criteria** (what must be TRUE):
  1. The HA Repairs UI shows active repair issues for: DB unreachable >5 min, buffer dropping records, HA recorder disabled; issues clear automatically when conditions resolve
  2. A `persistent_notification` appears when the worker thread dies and restarts, when backfill completes with a gap, and when backfill fails entirely
  3. `DataError` and `IntegrityError` trigger a per-row fallback loop; bad rows are logged and skipped; valid rows in the same batch still succeed
  4. `ProgrammingError` and `SyntaxError` (code bugs or schema drift) produce a critical-level log entry and a repair issue; no retry occurs
  5. The watchdog async task detects worker thread death, restarts the thread, and fires a `persistent_notification`; unhandled exceptions in the worker are caught and routed through the same restart path
  6. `strings.json` ships an `"issues"` section with title and description for every repair issue `translation_key`; no missing-key warnings appear in HA logs
```

Target (per D-01-c — criterion 3 removed, criterion 4 rewritten, "backfill failed entirely" in criterion 2 removed per D-09-b):

```markdown
**Success Criteria** (what must be TRUE):
  1. The HA Repairs UI shows active repair issues for: DB unreachable >5 min (`db_unreachable`), buffer dropping records (`buffer_dropping`, Phase 2), HA recorder disabled (`recorder_disabled`), states worker stalled (`states_worker_stalled`), meta worker stalled (`meta_worker_stalled`); issues clear automatically when conditions resolve
  2. A `persistent_notification` appears when the states or meta worker thread dies and restarts, when the orchestrator async task crashes and is relaunched, and when backfill encounters a recorder-retention gap
  3. Persistent DB write failures surface via `persistent_notification` after N=5 consecutive retries and as a `worker_stalled` repair issue; the issue clears automatically on the first successful operation after recovery. No error-class branching — `DataError`, `IntegrityError`, `ProgrammingError`, and `SyntaxError` all follow this unified path (see [Phase 3 CONTEXT D-01](phases/03-hardening-and-observability/03-CONTEXT.md))
  4. The retry decorator also wraps read paths (`read_watermark` on the worker connection, `_fetch_slice_raw` on the HA recorder pool); transient read errors retry silently with the same backoff schedule as writes
  5. The watchdog async task detects worker thread death, restarts the thread via spawn factories, and fires a structured `persistent_notification` with the captured `_last_exception` and `_last_context`; unhandled exceptions in worker `run()` are caught by an outer try/except and routed through the watchdog restart path
  6. `strings.json` ships an `"issues"` section with `title` and `description` for every repair issue `translation_key`; no missing-key warnings appear in HA logs
```

<!-- Existing ROADMAP.md Phase 3 Plans entry (line 80) -->

Current: `**Plans**: TBD`

Target: `**Plans**: 8 plans` with the list populated.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md — mark SQLERR-03 and SQLERR-04 DROPPED</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (ENTIRE file — the SQL Errors section at lines 52-56 and traceability table at lines 87-121)
    - .planning/phases/03-hardening-and-observability/03-CONTEXT.md (D-01 rationale — cite in the REQUIREMENTS update)
  </read_first>
  <action>
    Step 1 — replace the two SQLERR-03 and SQLERR-04 bullet entries (currently lines 54-55 of REQUIREMENTS.md) with the target text from `<interfaces>` above. Preserve the rest of the "SQL Errors (Granular error handling)" section and the other SQLERR entries (SQLERR-01, SQLERR-02, SQLERR-05) unchanged.

    Exact replacement lines:

    ```markdown
    - ~~**SQLERR-03**~~: ~~DataError / IntegrityError → isolate offending row via single-row fallback loop; log + skip bad row; continue batch~~ **DROPPED** — see [Phase 3 CONTEXT D-01](phases/03-hardening-and-observability/03-CONTEXT.md). Persistent bad-row failures stall the worker and surface via the `worker_stalled` repair issue + Phase 2 stall notification.
    - ~~**SQLERR-04**~~: ~~ProgrammingError / SyntaxError → critical log + repair issue; do not retry (code bug or schema drift)~~ **DROPPED** — see [Phase 3 CONTEXT D-01](phases/03-hardening-and-observability/03-CONTEXT.md). Persistent code-bug / schema-drift failures follow the same unified path as other errors: retry → stall notification after N=5 → `worker_stalled` repair issue.
    ```

    Step 2 — update the traceability table (section "## Traceability", lines 87+). Change the "Status" column for the following entries:

    - `WATCH-01`: `Pending` → `Shipped` (Phase 3).
    - `WATCH-03`: `Pending` → `Shipped` (Phase 3).
    - `OBS-01`: `Pending` → `Shipped` (Phase 3).
    - `OBS-02`: `Pending` → `Shipped` (Phase 3).
    - `OBS-03`: `Pending` → `Shipped` (Phase 3).
    - `OBS-04`: `Pending` → `Shipped` (Phase 3).
    - `SQLERR-03`: `Pending` → `DROPPED (see 03-CONTEXT.md#D-01)`.
    - `SQLERR-04`: `Pending` → `DROPPED (see 03-CONTEXT.md#D-01)`.

    Leave the WORK-*, BUF-*, BACK-*, META-*, SQLERR-01/02/05, and WATCH-02 rows UNCHANGED (Phase 1/2 carry-forward; their "Pending" status is a separate housekeeping item per 03-CONTEXT deferred list).

    Step 3 — update the coverage footer (around line 124):

    Current:
    ```markdown
    **Coverage:**
    - v1 requirements: 29 total (WORK×5, BUF×3, BACK×6, META×3, WATCH×3, OBS×4, SQLERR×5)
    - Mapped to phases: 29
    - Unmapped: 0 ✓
    ```

    Add a fourth bullet at the end:
    ```markdown
    - Dropped (scope reduction during phase planning): 2 (SQLERR-03, SQLERR-04 — see [Phase 3 CONTEXT D-01](phases/03-hardening-and-observability/03-CONTEXT.md))
    ```

    Do NOT otherwise touch the Coverage section counts — the 29-total math is unchanged because DROPPED requirements still COUNT in the total; they just ship with a DROPPED marker instead of implementation.

    Step 4 — update the `Last updated` footer line to today's date (2026-04-23) and add a note: `2026-04-23 — Phase 3 execution: SQLERR-03/04 marked DROPPED per D-01; WATCH/OBS entries marked Shipped`.
  </action>
  <verify>
    <automated>grep -c "DROPPED" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^\\- ~~\\*\\*SQLERR-03\\*\\*~~" .planning/REQUIREMENTS.md` outputs `1`.
    - `grep -cE "^\\- ~~\\*\\*SQLERR-04\\*\\*~~" .planning/REQUIREMENTS.md` outputs `1`.
    - `grep -c "03-CONTEXT.md#D-01" .planning/REQUIREMENTS.md` outputs >= `3` (SQLERR-03, SQLERR-04 bullets + coverage footer + traceability table rows).
    - `grep -cE "^\\| SQLERR-03 \\| Phase 3 \\| DROPPED" .planning/REQUIREMENTS.md` outputs `1`.
    - `grep -cE "^\\| SQLERR-04 \\| Phase 3 \\| DROPPED" .planning/REQUIREMENTS.md` outputs `1`.
    - `grep -cE "^\\| WATCH-01 \\| Phase 3 \\| Shipped" .planning/REQUIREMENTS.md` outputs `1`.
    - `grep -cE "^\\| WATCH-03 \\| Phase 3 \\| Shipped" .planning/REQUIREMENTS.md` outputs `1`.
    - `grep -cE "^\\| OBS-01 \\| Phase 3 \\| Shipped" .planning/REQUIREMENTS.md` outputs `1`.
    - `grep -cE "^\\| OBS-02 \\| Phase 3 \\| Shipped" .planning/REQUIREMENTS.md` outputs `1`.
    - `grep -cE "^\\| OBS-03 \\| Phase 3 \\| Shipped" .planning/REQUIREMENTS.md` outputs `1`.
    - `grep -cE "^\\| OBS-04 \\| Phase 3 \\| Shipped" .planning/REQUIREMENTS.md` outputs `1`.
    - The row for `WORK-01`, `BUF-01`, `BACK-01`, `META-01`, `SQLERR-01`, `WATCH-02` still contains `Pending` (regression check that only the 8 Phase 3 rows were changed):
      `grep -cE "^\\| (WORK-01|BUF-01|BACK-01|META-01|SQLERR-01|WATCH-02) \\| .* \\| Pending" .planning/REQUIREMENTS.md` outputs `6`.
    - `grep -c "Dropped (scope reduction during phase planning)" .planning/REQUIREMENTS.md` outputs `1`.
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md SQLERR-03/04 bullets show strikethrough + DROPPED + pointer to D-01; traceability table reflects Phase 3 outcome; coverage footer updated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md — rewrite Phase 3 success criteria + plans list</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (ENTIRE file; Phase 3 block at lines 69-80; progress table at line 84+)
    - .planning/phases/03-hardening-and-observability/03-CONTEXT.md (D-01-c, D-09-b — the specific rewrites required)
  </read_first>
  <action>
    Step 1 — replace the Phase 3 Success Criteria block (currently lines 73-79) with the target text from `<interfaces>` above (six numbered criteria; criterion 3 rewritten, criterion 4 rewritten, criterion 2 "backfill failed entirely" removed).

    Exact replacement for lines 73-79 (the `**Success Criteria** (what must be TRUE):` block through success criterion 6):

    ```markdown
    **Success Criteria** (what must be TRUE):
      1. The HA Repairs UI shows active repair issues for: DB unreachable >5 min (`db_unreachable`), buffer dropping records (`buffer_dropping`, Phase 2), HA recorder disabled (`recorder_disabled`), states worker stalled (`states_worker_stalled`), meta worker stalled (`meta_worker_stalled`); issues clear automatically when conditions resolve
      2. A `persistent_notification` appears when the states or meta worker thread dies and restarts, when the orchestrator async task crashes and is relaunched, and when backfill encounters a recorder-retention gap
      3. Persistent DB write failures surface via `persistent_notification` after N=5 consecutive retries and as a `worker_stalled` repair issue; the issue clears automatically on the first successful operation after recovery. No error-class branching — `DataError`, `IntegrityError`, `ProgrammingError`, and `SyntaxError` all follow this unified path (see [Phase 3 CONTEXT D-01](phases/03-hardening-and-observability/03-CONTEXT.md))
      4. The retry decorator also wraps read paths (`read_watermark` on the worker connection, `_fetch_slice_raw` on the HA recorder pool); transient read errors retry silently with the same backoff schedule as writes
      5. The watchdog async task detects worker thread death, restarts the thread via spawn factories, and fires a structured `persistent_notification` with the captured `_last_exception` and `_last_context`; unhandled exceptions in worker `run()` are caught by an outer try/except and routed through the watchdog restart path
      6. `strings.json` ships an `"issues"` section with `title` and `description` for every repair issue `translation_key`; no missing-key warnings appear in HA logs
    ```

    Step 2 — replace the `**Plans**: TBD` line (line 80) with:

    ```markdown
    **Plans**: 8 plans

    Plans:
    - [ ] 03-01-PLAN.md — Phase 3 constants + strings.json translation keys + issues.py helpers (4 create/clear pairs)
    - [ ] 03-02-PLAN.md — notifications.py module: notify_watchdog_recovery + notify_backfill_gap
    - [ ] 03-03-PLAN.md — Extend retry_until_success with on_recovery + on_sustained_fail hooks (TDD)
    - [ ] 03-04-PLAN.md — Wire Phase 3 hooks + _last_exception/_last_context in states_worker and meta_worker
    - [ ] 03-05-PLAN.md — backfill: recorder-retention gap detection + retry-wrap read paths
    - [ ] 03-06-PLAN.md — watchdog.py: watchdog_loop coroutine + spawn_states_worker/spawn_meta_worker factories
    - [ ] 03-07-PLAN.md — Wire watchdog + orchestrator done_callback + recorder_disabled check in __init__.py
    - [ ] 03-08-PLAN.md — Update REQUIREMENTS.md and ROADMAP.md per D-01 / D-12 (this plan)
    ```

    Step 3 — update the progress table at the bottom of ROADMAP.md (around line 86):

    Current row:
    ```markdown
    | 3. Hardening and Observability | 0/TBD | Not started | - |
    ```

    Replace with:
    ```markdown
    | 3. Hardening and Observability | 0/8 | In progress | - |
    ```

    (The row will be updated to `8/8 | Complete | <date>` by the execute-phase workflow once all 8 plans ship; Plan 08 itself only sets up the "8 plans" count and "In progress" status.)

    Step 4 — in the Milestones section at the top (around line 9-11), leave the `🚧 **v1.1 Robust Ingestion** - Phases 1-3 (in progress)` line unchanged — it correctly states in-progress. The phase checkbox `- [ ] **Phase 3: Hardening and Observability**` (line 15) ALSO stays unchecked until the full phase verification ships in a later workflow step.
  </action>
  <verify>
    <automated>grep -c "03-CONTEXT.md#D-01" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Persistent DB write failures surface via" .planning/ROADMAP.md` outputs `1`.
    - `grep -c "worker_stalled" .planning/ROADMAP.md` outputs >= `2` (once in criterion 1, once in criterion 3).
    - `grep -c "03-CONTEXT.md#D-01" .planning/ROADMAP.md` outputs >= `1`.
    - `grep -c "No error-class branching" .planning/ROADMAP.md` outputs `1`.
    - `grep -c "retry decorator also wraps read paths" .planning/ROADMAP.md` outputs `1`.
    - `grep -c "per-row fallback" .planning/ROADMAP.md` outputs `0` (criterion 3 rewrite removed the old "per-row fallback" wording).
    - `grep -c "critical-level log entry" .planning/ROADMAP.md` outputs `0` (criterion 4 rewrite removed the old code-bug halt wording).
    - `grep -c "\\*\\*Plans\\*\\*: 8 plans" .planning/ROADMAP.md` outputs `1`.
    - `grep -cE "^\\- \\[ \\] 03-0[1-8]-PLAN\\.md" .planning/ROADMAP.md` outputs `8`.
    - `grep -cE "\\| 3\\. Hardening and Observability \\| 0/8 \\| In progress" .planning/ROADMAP.md` outputs `1`.
  </acceptance_criteria>
  <done>
    ROADMAP.md Phase 3 success criteria rewritten to reflect shipped architecture; plans list populated; progress table shows 0/8 In progress.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REQUIREMENTS.md / ROADMAP.md | Planning documents — no runtime trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08-01 | Tampering | DROPPED markers in REQUIREMENTS.md | accept | Source-controlled documents; changes are reviewed via git history |
| T-03-08-02 | Information Disclosure | ROADMAP success criteria reveal implementation details | accept | Planning documents are internal developer artifacts; no sensitive data |
</threat_model>

<verification>
- Sanity grep: `grep -c "DROPPED" .planning/REQUIREMENTS.md` outputs >= `3`.
- Sanity grep: `grep -c "worker_stalled" .planning/ROADMAP.md` outputs >= `2`.
- Cross-reference: both documents now point to `phases/03-hardening-and-observability/03-CONTEXT.md` for the D-01 rationale.
- No code tests to run — this is a docs-only plan.
</verification>

<success_criteria>
- REQUIREMENTS.md shows SQLERR-03 and SQLERR-04 as DROPPED with strikethrough AND a pointer to D-01.
- REQUIREMENTS.md traceability table updated for all 8 Phase 3 requirement IDs (6 Shipped, 2 DROPPED).
- ROADMAP.md Phase 3 Success Criteria rewritten per D-01-c + D-09-b.
- ROADMAP.md Phase 3 Plans list populated with the 8 shipped plans.
- ROADMAP.md progress table reflects 0/8 In progress.
- No regressions elsewhere in either file (WORK/BUF/BACK/META carry-forward unchanged).
</success_criteria>

<output>
After completion, create `.planning/phases/03-hardening-and-observability/03-08-SUMMARY.md` documenting:
- Exact bullet format used for DROPPED markers in REQUIREMENTS.md
- Final Phase 3 Success Criteria text (copied verbatim — serves as the "what shipped" record)
- Links to D-01 / D-09-b / D-12 in 03-CONTEXT.md for cross-reference
</output>
