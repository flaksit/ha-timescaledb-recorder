---
phase: quick-260427-oi6
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/timescaledb_recorder/states_worker.py
  - tests/test_states_worker.py
autonomous: true
requirements: [FIX-12]

must_haves:
  truths:
    - "Schema setup in _run_main_loop uses self._setup_schema() — no bare try/except"
    - "read_all_known_entities is the retry-wrapped form of _read_all_known_entities_raw"
    - "All remaining bare except blocks have noqa + a motivating comment"
    - "New tests assert retry wiring for _setup_schema and read_all_known_entities"
    - "All existing tests still pass"
  artifacts:
    - path: "custom_components/timescaledb_recorder/states_worker.py"
      provides: "retry-wrapped _setup_schema and read_all_known_entities"
      contains: "_setup_schema_raw"
    - path: "tests/test_states_worker.py"
      provides: "retry coverage tests for new wrappers"
  key_links:
    - from: "__init__"
      to: "retry_until_success"
      via: "self._setup_schema = retry_until_success(...)(self._setup_schema_raw)"
    - from: "__init__"
      to: "retry_until_success"
      via: "self.read_all_known_entities = retry_until_success(...)(self._read_all_known_entities_raw)"
---

<objective>
Wrap the two remaining unprotected DB calls in states_worker.py with retry_until_success,
and verify the coverage with targeted tests (Issue #12).

Purpose: Every DB call in the worker must be retry-decorated; bare exceptions must have
explicit motivating comments so the "all bare exceptions must be justified" rule is self-evident
on code review.

Output: Updated states_worker.py (two new _raw methods + two new retry wrappers in __init__),
updated test_states_worker.py (two new retry-wiring tests + one retry-behaviour test per wrapper).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor states_worker.py — extract raw methods, add retry wrappers</name>
  <files>custom_components/timescaledb_recorder/states_worker.py</files>
  <action>
Make the following changes to states_worker.py. Do NOT change any other logic.

1. Add `_setup_schema_raw(self)` method (private, sync) in the "Main loop" section,
   containing exactly the body of the current try block at lines 275-286 — the
   `conn = self.get_db_connection()` call and the `sync_setup_schema(conn, ...)` call.
   Add a docstring: "Raw schema setup — wrapped by retry_until_success in __init__."

2. In `__init__`, after the `self.read_watermark = ...` assignment (line 139), add:

   ```
   # Schema setup: retry on transient DB failure at startup (Issue #12).
   # No stall/recovery hooks — schema setup is a one-time startup op; a
   # persistent failure stalls the thread until DB is available (acceptable:
   # the thread cannot do useful work without schema anyway).
   self._setup_schema = retry_until_success(
       stop_event=stop_event,
       on_transient=self.reset_db_connection,
   )(self._setup_schema_raw)
   ```

3. In `_run_main_loop`, replace the entire try/except block at lines 273-286 (the
   bare try for schema setup, including the preceding comment block starting "Ensure
   schema (idempotent…)") with:

   ```
   self._last_op = "schema_setup"
   self._setup_schema()
   ```

   The comment block that begins "Ensure schema (idempotent; D-15-d carries Phase 1
   behaviour). Retry via decorator would be overkill here …" must be REMOVED — the
   reasoning it gave is now wrong per Issue #12.

4. Rename `read_all_known_entities` (lines 419-430) to `_read_all_known_entities_raw`.
   Update its docstring first line to: "Raw read — wrapped by retry_until_success in __init__."

5. In `__init__`, after the `self._setup_schema = ...` block added in step 2, add:

   ```
   # read_all_known_entities: called by orchestrator via executor job at backfill
   # start; must retry on transient errors to avoid a bare DB failure escaping
   # to the async caller (Issue #12). Same minimal shape as read_watermark.
   self.read_all_known_entities = retry_until_success(
       stop_event=stop_event,
       on_transient=self.reset_db_connection,
   )(self._read_all_known_entities_raw)
   ```

6. Verify all remaining `except Exception:  # noqa: BLE001` sites carry a short
   motivating comment explaining why the broad catch is intentional:
   - `reset_db_connection` swallow (line 157): comment should say "connection may
     already be dead; close() may raise — nothing useful we can do here"
   - `run()` finally teardown swallow (line 259): already has "Errors here are caught
     and logged, NOT re-raised (D-06 principle)" — leave as-is.
   - `run()` outer catch (line 239): already has "last-resort guard (D-06-a)" — leave as-is.
   - Shutdown best-effort flush (line 359): already has "intentional best-effort; rows
     lost on shutdown accepted" comment form — leave as-is.
   For `reset_db_connection`, if the current comment is absent or vague, add the
   motivation inline: "# Connection may already be dead; close() can raise — swallow
   so reset always completes."
  </action>
  <verify>
    <automated>cd /home/janfr/dev/home-assistant/ha-timescaledb-recorder && uv run python -c "from custom_components.timescaledb_recorder.states_worker import TimescaledbStateRecorderThread; print('import ok')"</automated>
  </verify>
  <done>
    states_worker.py imports cleanly; _setup_schema_raw and _read_all_known_entities_raw
    exist as methods; _run_main_loop has no try/except for schema setup; __init__ assigns
    self._setup_schema and self.read_all_known_entities via retry_until_success.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add retry-wiring and retry-behaviour tests</name>
  <files>tests/test_states_worker.py</files>
  <action>
Add the following tests to the bottom of tests/test_states_worker.py, in a new section
comment "# Issue #12: retry wiring for _setup_schema and read_all_known_entities".

Follow the exact same patterns as the existing read_watermark retry tests
(test_read_watermark_is_retry_wrapped, test_read_watermark_retries_on_transient_error,
test_read_watermark_retry_wiring_captures_on_transient). Do NOT duplicate test logic;
write three tests per new wrapper:

### For _setup_schema / _setup_schema_raw:

1. `test_setup_schema_is_retry_wrapped`
   Assert `t._setup_schema is not t._setup_schema_raw` and
   `hasattr(t, "_setup_schema_raw")`.

2. `test_setup_schema_retries_on_transient_error`
   Use the same fail-once-succeed-second pattern as
   `test_read_watermark_retries_on_transient_error`. Patch `_setup_schema_raw` on the
   instance (side_effect: raises psycopg.OperationalError on first call, returns None on
   second). Rebuild `t._setup_schema` with `backoff_schedule=(0,)`. Call `t._setup_schema()`.
   Assert `reset_db_connection` was called and no exception escaped.

3. `test_setup_schema_retry_wiring_captures_on_transient`
   Use the `fake_retry` / `patch` pattern from
   `test_read_watermark_retry_wiring_captures_on_transient`. After constructing the thread
   with the patched retry, capture all calls to fake_retry.
   Find the call whose wrapped function is `_setup_schema_raw` (use the call index — it
   will be the third call since __init__ now calls retry_until_success three times: insert,
   watermark, schema, read_all_known_entities). Assert `on_transient.__func__` is
   `TimescaledbStateRecorderThread.reset_db_connection` and `on_recovery` is None and
   `on_sustained_fail` is None.

### For read_all_known_entities / _read_all_known_entities_raw:

4. `test_read_all_known_entities_is_retry_wrapped`
   Assert `t.read_all_known_entities is not t._read_all_known_entities_raw` and
   `hasattr(t, "_read_all_known_entities_raw")`.

5. `test_read_all_known_entities_retries_on_transient_error`
   Same fail-once pattern. Side effect: raises psycopg.OperationalError first, returns
   `{"sensor.a", "sensor.b"}` second (from a mock cursor). Rebuild wrapper with
   `backoff_schedule=(0,)`. Call `t.read_all_known_entities()`. Assert result ==
   `{"sensor.a", "sensor.b"}` and reset was called.

6. `test_read_all_known_entities_retry_wiring_captures_on_transient`
   Same `fake_retry` pattern. Assert the call that wraps `_read_all_known_entities_raw`
   has `on_transient.__func__ is reset_db_connection` and no hooks.

Note: the existing `test_read_all_known_entities_returns_set_of_ids` calls
`t.read_all_known_entities()` directly — it still works because the retry wrapper
passes through to the raw method on success. No change needed to that test.
  </action>
  <verify>
    <automated>cd /home/janfr/dev/home-assistant/ha-timescaledb-recorder && uv run pytest tests/test_states_worker.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    All tests pass including the six new ones; no existing tests broken.
  </done>
</task>

</tasks>

<verification>
uv run pytest tests/test_states_worker.py -x -q
</verification>

<success_criteria>
- states_worker.py has no bare try/except for schema setup in _run_main_loop
- __init__ wraps _setup_schema_raw and _read_all_known_entities_raw with retry_until_success
- All remaining noqa: BLE001 sites have motivating inline comments
- All new tests pass; all pre-existing tests pass
- Module imports cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260427-oi6-ensure-retry-decorator-used-everywhere-d/260427-oi6-01-SUMMARY.md`
</output>
