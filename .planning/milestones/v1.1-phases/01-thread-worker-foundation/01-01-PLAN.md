---
phase: 01-thread-worker-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/timescaledb_recorder/const.py
autonomous: true
requirements:
  - WORK-03
  - WORK-04

must_haves:
  truths:
    - "Every SQL constant in const.py uses %s positional placeholders (no $N tokens remain)"
    - "Four new SELECT_*_CURRENT_SQL constants exist for change-detection reads (per CLAUDE.md: all SQL in const.py)"
    - "Python .format() placeholders ({chunk_days}, {compress_hours}, {schedule_hours}) are unchanged"
  artifacts:
    - path: "custom_components/timescaledb_recorder/const.py"
      provides: "All SQL constants with psycopg3 %s syntax"
      contains: "SELECT_ENTITY_CURRENT_SQL"
  key_links:
    - from: "const.py INSERT_SQL"
      to: "worker.py _flush()"
      via: "executemany(INSERT_SQL, rows)"
      pattern: "%s.*%s.*%s.*%s.*%s"
    - from: "const.py SELECT_*_CURRENT_SQL"
      to: "syncer.py _*_row_changed()"
      via: "cur.execute(SELECT_ENTITY_CURRENT_SQL, (entity_id,))"
      pattern: "SELECT_ENTITY_CURRENT_SQL"
---

<objective>
Migrate all SQL placeholder syntax in `const.py` from asyncpg `$N` positional parameters to psycopg3 `%s` positional parameters, and add four new SELECT constants for change-detection reads (moving inline SQL out of `syncer.py` per CLAUDE.md convention).

Purpose: Every downstream plan (`worker.py`, `schema.py`, `ingester.py`, `syncer.py`) depends on correct `%s` placeholders. A single missed `$N` causes a `psycopg.errors.SyntaxError` at runtime. This plan eliminates that risk before any other code is written.

Output: `const.py` with all `$N` replaced by `%s` and four `SELECT_*_CURRENT_SQL` constants added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-thread-worker-foundation/01-CONTEXT.md
@.planning/phases/01-thread-worker-foundation/01-RESEARCH.md

<interfaces>
<!-- Existing const.py SQL constants and their placeholder counts (from codebase read). -->
<!-- Executor: migrate EXACTLY these constants. Do not touch DDL-only constants. -->

Constants requiring $N → %s migration and their replacement counts:
  INSERT_SQL:                  5 replacements  ($1 $2 $3 $4 $5)
  SCD2_CLOSE_ENTITY_SQL:       2 replacements  ($1 $2)
  SCD2_CLOSE_DEVICE_SQL:       2 replacements  ($1 $2)
  SCD2_CLOSE_AREA_SQL:         2 replacements  ($1 $2)
  SCD2_CLOSE_LABEL_SQL:        2 replacements  ($1 $2)
  SCD2_SNAPSHOT_ENTITY_SQL:   14 replacements  ($1..$13 + repeated $1 in subquery)
  SCD2_SNAPSHOT_DEVICE_SQL:    9 replacements  ($1..$8 + repeated $1 in subquery)
  SCD2_SNAPSHOT_AREA_SQL:      5 replacements  ($1..$4 + repeated $1 in subquery)
  SCD2_SNAPSHOT_LABEL_SQL:     6 replacements  ($1..$5 + repeated $1 in subquery)
  SCD2_INSERT_ENTITY_SQL:     13 replacements  ($1..$13)
  SCD2_INSERT_DEVICE_SQL:      8 replacements  ($1..$8)
  SCD2_INSERT_AREA_SQL:        4 replacements  ($1..$4)
  SCD2_INSERT_LABEL_SQL:       5 replacements  ($1..$5)

LEAVE UNCHANGED (Python .format() parameters — NOT psycopg parameters):
  CREATE_HYPERTABLE_SQL:       {chunk_days}
  ADD_COMPRESSION_POLICY_SQL:  {compress_hours}, {schedule_hours}

New constants to ADD (SQL extracted from syncer.py inline strings):
  SELECT_ENTITY_CURRENT_SQL — SELECT from entities WHERE entity_id = %s AND valid_to IS NULL
  SELECT_DEVICE_CURRENT_SQL — SELECT from devices WHERE device_id = %s AND valid_to IS NULL
  SELECT_AREA_CURRENT_SQL   — SELECT from areas WHERE area_id = %s AND valid_to IS NULL
  SELECT_LABEL_CURRENT_SQL  — SELECT from labels WHERE label_id = %s AND valid_to IS NULL
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate $N placeholders to %s and add SELECT constants</name>
  <files>custom_components/timescaledb_recorder/const.py</files>
  <read_first>
    - custom_components/timescaledb_recorder/const.py (current state — read entirely before any edit)
    - custom_components/timescaledb_recorder/syncer.py (lines 258-331: the four _*_row_changed methods to extract inline SQL from)
  </read_first>
  <action>
In `const.py`, perform two changes:

**Change 1 — Placeholder migration:**
Replace every `$N` token with `%s` in the 13 parameterized SQL constants listed in the interface block above. The positional order of parameters does NOT change — only the token syntax changes. After migration:
- `INSERT_SQL` must contain exactly five `%s` tokens and zero `$` characters
- Each `SCD2_CLOSE_*_SQL` must contain exactly two `%s` tokens
- Each `SCD2_SNAPSHOT_*_SQL` must use `%s` throughout (including in the WHERE NOT EXISTS subquery — psycopg3 handles repeated `%s` as separate positional parameters unlike asyncpg's `$1` reuse)
- Each `SCD2_INSERT_*_SQL` must use `%s` for every column value position

IMPORTANT: The `SCD2_SNAPSHOT_*_SQL` constants currently reuse `$1` in the WHERE NOT EXISTS subquery (e.g., `WHERE entity_id = $1 AND valid_to IS NULL`). In psycopg3, each `%s` is a separate positional slot. The snapshot SQL must therefore pass the first param (entity_id/device_id/area_id/label_id) TWICE in the tuple when executing. Document this in a comment directly above each SNAPSHOT constant: `# NOTE: first positional param (%s for id) appears TWICE — once in SELECT, once in WHERE NOT EXISTS subquery.`

Do not modify: `CREATE_HYPERTABLE_SQL`, `ADD_COMPRESSION_POLICY_SQL` (these use Python `.format()` `{}` syntax — safe and intentional).

Do not modify: any DDL constants without `$N` parameters (`CREATE_TABLE_SQL`, `CREATE_INDEX_SQL`, `SET_COMPRESSION_SQL`, etc.).

**Change 2 — Add four SELECT constants:**
After the existing `SCD2_INSERT_LABEL_SQL` constant, add a new section with header comment and four constants:

```python
# Change-detection SELECT constants — read the current open row for each registry type.
# Moved from inline strings in syncer.py per project convention (all SQL in const.py).
# %s = the registry ID (entity_id / device_id / area_id / label_id).
SELECT_ENTITY_CURRENT_SQL = (
    "SELECT name, platform, device_id, area_id, labels, device_class,"
    " unit_of_measurement, disabled_by, extra"
    " FROM entities WHERE entity_id = %s AND valid_to IS NULL"
)

SELECT_DEVICE_CURRENT_SQL = (
    "SELECT name, manufacturer, model, area_id, labels, extra"
    " FROM devices WHERE device_id = %s AND valid_to IS NULL"
)

SELECT_AREA_CURRENT_SQL = (
    "SELECT name, extra FROM areas WHERE area_id = %s AND valid_to IS NULL"
)

SELECT_LABEL_CURRENT_SQL = (
    "SELECT name, color, extra FROM labels WHERE label_id = %s AND valid_to IS NULL"
)
```

No other changes to the file.
  </action>
  <verify>
    <automated>
      # Verify: no $N placeholders remain in the parameterized SQL constants
      python3 -c "
import ast, re, sys
src = open('custom_components/timescaledb_recorder/const.py').read()
dollar_matches = re.findall(r'\\\$[0-9]+', src)
if dollar_matches:
    print('FAIL: found dollar placeholders:', dollar_matches)
    sys.exit(1)
# Verify four new SELECT constants exist
for name in ['SELECT_ENTITY_CURRENT_SQL', 'SELECT_DEVICE_CURRENT_SQL', 'SELECT_AREA_CURRENT_SQL', 'SELECT_LABEL_CURRENT_SQL']:
    if name not in src:
        print('FAIL: missing constant', name)
        sys.exit(1)
# Verify .format() placeholders still present
for token in ['{chunk_days}', '{compress_hours}', '{schedule_hours}']:
    if token not in src:
        print('FAIL: format placeholder removed:', token)
        sys.exit(1)
print('PASS')
"
    </automated>
  </verify>
  <done>
    - `const.py` contains zero `$N` tokens
    - `SELECT_ENTITY_CURRENT_SQL`, `SELECT_DEVICE_CURRENT_SQL`, `SELECT_AREA_CURRENT_SQL`, `SELECT_LABEL_CURRENT_SQL` are exported
    - `{chunk_days}`, `{compress_hours}`, `{schedule_hours}` format tokens unchanged
    - All 13 migrated constants have `%s` placeholders in correct positions
    - Each `SCD2_SNAPSHOT_*_SQL` has a comment noting the duplicate first parameter
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| const.py → psycopg3 cursor | SQL strings are concatenated via f-string with TABLE_NAME constant; no user input reaches SQL construction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | const.py f-string TABLE_NAME | accept | TABLE_NAME is a module-level constant (`"states"`), never derived from user input; no injection vector exists |
| T-01-02 | Information Disclosure | const.py SQL structure | accept | SQL is static, source-controlled; no runtime secrets in SQL strings |
</threat_model>

<verification>
Run the automated check from the task verify block. Then visually confirm INSERT_SQL shows `VALUES (%s, %s, %s, %s, %s)`.
</verification>

<success_criteria>
`python3 -c "from custom_components.timescaledb_recorder.const import SELECT_ENTITY_CURRENT_SQL, INSERT_SQL; import re; assert not re.search(r'\$[0-9]', INSERT_SQL); print('OK')"` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/01-thread-worker-foundation/01-01-SUMMARY.md`
</output>
